<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.home_activity_fragments.WeatherDeepDetailsFragment"
        android:orientation="vertical"
        android:background="@drawable/rainy">

        <include
            android:id="@+id/weatherDeepInfoTop"
            layout="@layout/weather_deep_info_top"
            android:layout_width="match_parent"
            android:layout_margin="10dp"
            android:layout_height="400dp"/>

        <include
            android:padding="20dp"
            android:id="@+id/weatherDeepInfoBottom"
            layout="@layout/weather_deep_info_bottom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:layout_gravity="center"
            android:backgroundTint="@color/white" />


    </LinearLayout>
</layout>